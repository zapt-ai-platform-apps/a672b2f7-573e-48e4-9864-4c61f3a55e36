import { fetchSquads, createSquad, updateSquad } from "./squadApiFunctions";

export { fetchSquads, createSquad, updateSquad };

export const squadServiceApi = {
  fetchSquads,
  createSquad,
  updateSquad
};